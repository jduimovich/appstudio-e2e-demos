apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: single-container-app 
  labels:
    app: single-container-app-service
spec: 
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: single-container-app-service
    weight: 100
