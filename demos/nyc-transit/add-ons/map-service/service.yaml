apiVersion: v1
kind: Service
metadata: 
  labels: 
    app.kubernetes.io/instance: map-service 
  name: map-service 
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app.kubernetes.io/instance: map-service 
